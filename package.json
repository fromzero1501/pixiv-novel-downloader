{
  "name": "local-collection-library",
  "private": true,
  "version": "1.2.3",
  "type": "module",
  "scripts": {
    "dev": "node scripts/build-help-doc.mjs && vite",
    "build": "node scripts/build-help-doc.mjs && node scripts/reset-dist.mjs && vite build",
    "tauri": "tauri",
    "portable": "tauri build && node scripts/pack-portable.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-dialog": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "vite": "^6.0.0"
  }
}
